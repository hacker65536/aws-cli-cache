# AWS CLI Cache - 仕様書

## 概要

AWS CLI Cache は、AWS CLI の API コール回数を削減するためのキャッシュレイヤーです。TTL ベースの有効期限管理、LRU 削除、整合性検証をサポートし、本番環境での使用に最適化されています。

**バージョン**: 4.0.0  
**最終更新**: 2025 年 12 月 19 日

---

## アーキテクチャ

### モジュール構成

v4.0.0 でモジュラー設計に移行し、テスト可能な構造になりました。

```
aws_cache.sh          # エントリーポイント（モジュールをsource）
lib/
├── config.sh         # 環境変数と設定の初期化
├── excludes.sh       # キャッシュ除外ルールの管理
├── extract.sh        # AWS CLIコマンドからのパラメータ抽出
├── hash.sh           # ハッシュ生成（params_hash, cache_key）
├── cache_io.sh       # キャッシュファイルの読み書き
├── limits.sh         # LRU削除とサイズ制限
├── stats.sh          # 統計記録と表示
├── core.sh           # メインのaws_cached関数
└── cli.sh            # CLIコマンド（clear, clean, stats等）
```

### 主要な設計原則

1. **モジュラー設計**: 9 つのモジュールに分割されたテスト可能な構造
2. **階層化**: 6 層の論理的な構造（profile/service/region/action/params_hash/format）
3. **並行性**: アトミック操作と PID ベース命名による安全な並行実行
4. **効率性**: LRU 削除とメモリキャッシュによる最適化
5. **安全性**: 整合性検証とエラーハンドリング
6. **拡張性**: プラグイン可能な除外ルール
7. **クロスプラットフォーム**: macOS/Linux 両対応

---

## 主要機能

### 1. 階層化キャッシュ構造

6 層の論理的な階層構造でキャッシュを管理：

```
{profile}/{service}/{region}/{action}/{params_hash}/{format}/
  └── {hash}_{ttl}_{pid}.cache
```

**ファイル名形式**: `{hash}_{ttl}_{pid}.cache`

- `hash`: SHA256 ハッシュ（64 文字）- コマンド全体のハッシュ
- `ttl`: Time To Live（秒）- 有効期限
- `pid`: Process ID - 並行実行時の衝突回避

**params_hash（第 5 層）**: SHA256 ハッシュの先頭 16 文字（--region, --profile, --output を除くパラメータから生成）

### 2. TTL ベースの有効期限管理

- **デフォルト TTL**: 3600 秒（1 時間）
- **カスタム TTL**: コマンド単位で指定可能
- **自動判定**: ファイル名から有効期限を計算
- **メタデータ不要**: すべての情報をファイル名に埋め込み

### 3. LRU 削除（Least Recently Used）

キャッシュサイズ制限に達した場合、古いファイルから自動削除：

- **最大ファイル数**: 10,000（デフォルト）
- **最大サイズ**: 1GB（デフォルト）
- **削除タイミング**: キャッシュ書き込み前（確率的チェック: 1%の確率で実行）
- **削除基準**: アクセス時刻が古い順
- **削減目標**: 制限の 80%まで削減

### 4. 整合性検証

SHA256 ハッシュによるファイル整合性チェック：

- **ハッシュファイル**: `.sha256`拡張子
- **検証タイミング**: キャッシュ読み込み時
- **改ざん検出**: ハッシュ不一致時は自動削除
- **オプション機能**: デフォルトでは無効（パフォーマンス優先）

### 5. キャッシュ除外ルール

Write 系操作は自動的にキャッシュから除外：

**デフォルト除外**:

- Create 系: `create-*`, `run-*`
- Update 系: `modify-*`, `update-*`
- Delete 系: `delete-*`, `terminate-*`
- 実行系: `invoke`, `publish`, `send-*`

**カスタム除外**:

- 設定ファイル: `~/.config/aws-cli/cache-exclude`
- ワイルドカード対応: `service:*`, `*:action`

### 6. 並行実行対策

複数プロセスでの同時実行に対応：

- **アトミック書き込み**: 一時ファイル → mv
- **PID ベースのファイル名**: 衝突回避
- **ファイルロック不要**: ファイルシステムの原子性を利用

### 7. 統計情報の記録

キャッシュヒット率の分析が可能：

- **記録内容**: タイムスタンプ、ヒット/ミス
- **保存場所**: `{cache_dir}/.stats`
- **メトリクス**: 総リクエスト数、ヒット率
- **非同期実行**: バックグラウンドで記録（パフォーマンス影響を最小化）

---

## 環境変数

### 必須環境変数

なし（すべてオプション）

### オプション環境変数

| 変数名                     | デフォルト値                      | 説明                      |
| -------------------------- | --------------------------------- | ------------------------- |
| `AWS_CACHE_DIR`            | `~/.cache/aws-cli`                | キャッシュディレクトリ    |
| `AWS_CACHE_TTL`            | `3600`                            | デフォルト TTL（秒）      |
| `AWS_CACHE_MAX_SIZE`       | `1073741824`                      | 最大サイズ（バイト、1GB） |
| `AWS_CACHE_MAX_FILES`      | `10000`                           | 最大ファイル数            |
| `AWS_CACHE_VERIFY`         | `false`                           | 整合性チェック有効化      |
| `AWS_CACHE_STATS`          | `false`                           | 統計記録有効化            |
| `AWS_CACHE_EXCLUDE_CONFIG` | `~/.config/aws-cli/cache-exclude` | 除外設定ファイル          |

### AWS 環境変数（参照のみ）

| 変数名                | 用途                 |
| --------------------- | -------------------- |
| `AWS_PROFILE`         | プロファイル名の取得 |
| `AWS_DEFAULT_PROFILE` | プロファイル名の代替 |
| `AWS_REGION`          | リージョン名の取得   |
| `AWS_DEFAULT_REGION`  | リージョン名の代替   |

---

## コマンドラインインターフェース

### メイン関数

```bash
aws_cached [options] <aws-cli-command>
```

**オプション**:

- `--cache-ttl <seconds>` - TTL を指定
- `--force-refresh` - キャッシュを無視して更新
- `--no-cache` - キャッシュをバイパス
- `--verbose` - デバッグ情報を表示

### 管理コマンド

```bash
./aws_cache.sh <command> [arguments]
```

| コマンド         | 引数       | 説明                     |
| ---------------- | ---------- | ------------------------ |
| `clear`          | `[target]` | キャッシュをクリア       |
| `clean`          | なし       | 期限切れキャッシュを削除 |
| `stats`          | `[target]` | 統計情報を表示           |
| `metrics`        | なし       | ヒット率メトリクスを表示 |
| `tree`           | なし       | ディレクトリ構造を表示   |
| `excludes`       | なし       | 除外ルールを表示         |
| `add-exclude`    | `<rule>`   | 除外ルールを追加         |
| `remove-exclude` | `<rule>`   | 除外ルールを削除         |
| `test`           | なし       | 動作テストを実行         |

---

## パフォーマンス特性

### ベンチマーク結果

**テスト環境**: macOS, AWS CLI v2, `sts get-caller-identity`

| 実行方法         | 実行時間 | 比較 |
| ---------------- | -------- | ---- |
| 直接 AWS CLI     | 1732ms   | 100% |
| キャッシュミス   | 2112ms   | 122% |
| キャッシュヒット | 510ms    | 29%  |

**高速化率**: キャッシュヒット時 約 3.4 倍

※ キャッシュミス時は AWS CLI 実行 + キャッシュ書き込みのオーバーヘッドあり

### オーバーヘッド

| 機能               | 書き込み | 読み込み |
| ------------------ | -------- | -------- |
| サイズ制限チェック | +5-10ms  | なし     |
| 整合性チェック     | +10-20ms | +5-10ms  |
| 統計記録           | +1-2ms   | +1-2ms   |

### スケーラビリティ

**大規模環境での想定**:

- 10,000 キャッシュエントリ
- ディスク使用量: ~100MB
- inode 使用数: ~20,000（ファイル + 6 層ディレクトリ）
- 検索時間: ~100ms

---

## セキュリティ

### 実装済みの対策

1. **パストラバーサル対策**

   - `${CACHE_DIR:?}` による空変数チェック
   - 相対パスの適切な処理

2. **ファイル権限**

   - デフォルトの umask 設定を使用
   - ユーザーディレクトリ内に配置

3. **インジェクション対策**

   - 変数の適切なクォート
   - コマンド実行時の引数検証

4. **整合性検証**

   - SHA256 ハッシュによる改ざん検出
   - オプション機能として提供

5. **アトミック操作**
   - 一時ファイル経由の書き込み
   - 並行実行時の競合回避

### 潜在的なリスクと対策

| リスク                 | 対策                   |
| ---------------------- | ---------------------- |
| キャッシュポイズニング | 整合性チェックを有効化 |
| ディスク容量枯渇       | サイズ制限を設定       |
| 機密情報の漏洩         | 除外ルールで対応       |

---

## 互換性

### OS 互換性

| OS                 | 対応状況    | 備考              |
| ------------------ | ----------- | ----------------- |
| macOS              | ✅ 完全対応 | テスト済み        |
| Linux              | ✅ 完全対応 | テスト済み        |
| Windows (WSL)      | ⚠️ 動作想定 | 未テスト          |
| Windows (Git Bash) | ❌ 非対応   | Bash 拡張機能使用 |

### Shell 互換性

| Shell     | 対応状況    | 備考          |
| --------- | ----------- | ------------- |
| Bash 4.0+ | ✅ 推奨     | 完全対応      |
| Bash 3.2+ | ⚠️ 制限あり | 一部機能制限  |
| Zsh       | ✅ 完全対応 | テスト済み    |
| Dash      | ❌ 非対応   | Bash 拡張使用 |

### AWS CLI 互換性

| バージョン | 対応状況        |
| ---------- | --------------- |
| AWS CLI v1 | ✅ 対応         |
| AWS CLI v2 | ✅ 対応（推奨） |

### プラットフォーム互換性の注意点

macOS (BSD) と Linux (GNU) でコマンドの動作が異なるため、以下の制約があります：

- `date +%s%N` は使用禁止（macOS 非対応）
- `xargs -r` は使用禁止（macOS 非対応）
- `find -printf` は使用禁止（macOS 非対応）
- `stat` コマンドは `$OSTYPE` で分岐

---

## 制限事項

### 既知の制限

1. **ファイル数制限**

   - デフォルト: 10,000 ファイル
   - 超過時: LRU 削除

2. **ディスク使用量制限**

   - デフォルト: 1GB
   - 超過時: LRU 削除

3. **パス長制限**

   - 一部のファイルシステムで問題の可能性
   - 深い階層構造（6 層）

4. **統計ファイルサイズ**
   - `.stats`ファイルは無制限に増加
   - 定期的な削除を推奨

### 非対応機能

1. **圧縮機能** - gzip による圧縮は未実装
2. **メモリキャッシュ** - 頻繁にアクセスされるデータのメモリ保持
3. **Web UI** - グラフィカルな管理インターフェース

---

## コード品質

### 準拠規格

- ✅ **Google Shell Style Guide**: 完全準拠
- ✅ **XDG Base Directory Specification**: 準拠
- ✅ **Shellcheck**: 警告最小化

### テストカバレッジ

| テストスイート       | テスト数 | 状態 |
| -------------------- | -------- | ---- |
| ユニットテスト       | 42       | ✅   |
| 機能テスト           | 42       | ✅   |
| 統合テスト           | 19       | ✅   |
| パフォーマンステスト | -        | ✅   |

**合計**: 103 テスト

### コードメトリクス

| 項目         | 値      |
| ------------ | ------- |
| モジュール数 | 9       |
| 総行数       | ~900 行 |
| コメント率   | ~25%    |
| 複雑度       | 低〜中  |

---

## 依存関係

### 必須コマンド

- `bash` (4.0+)
- `aws` (AWS CLI)
- `find`
- `grep`
- `sed`
- `awk`
- `shasum` (SHA256 計算)
- `du` (ディスク使用量)

### オプションコマンド

- `tree` (ディレクトリ構造表示)
- `jq` (JSON 検証、テスト用)

---

## ライセンス

MIT License - 詳細は [LICENSE](LICENSE) を参照

---

**作成者**: Kiro AI Assistant  
**バージョン**: 4.0.0
